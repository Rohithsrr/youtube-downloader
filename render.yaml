services:
  - type: web
    name: youtube-downloader
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: YOUTUBE_COOKIES
        sync: false
